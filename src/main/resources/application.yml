quarkus:
  http:
    http2: true
    root-path: /api/v1
    port: 9905
    io-threads: 4                    # 2x CPU cores (1-core pod)
    limits:
      max-body-size: 10M
      max-connections: 500           # Right-sized for 300 TPS/pod

#  datasource:
#    db-kind: oracle
#    username: aaa
#    password: Aaa!89nky78D
#    reactive:
#      #url: oracle:thin:@localhost:1521/FREEPDB1
#      url: oracle:thin:@localhost:1522/ORCL

  datasource:
    db-kind: oracle
    username: aaa
    password: Aaa!89nky78D
    reactive:
      url: oracle:thin:@aaa-dev.cn8eyeqma3tp.ap-southeast-2.rds.amazonaws.com:1521/ORCL


  # Vertx Configuration - Optimized for 1500 TPS (5 pods x 1 core, 300 TPS/pod)
  vertx:
    event-loops-pool-size: 4         # 2x CPU cores (1 core per pod)
    internal-blocking-pool-size: 32  # Supports 24 concurrent consumer handlers + headroom
    warning-exception-time: 10s
    max-event-loop-execute-time: 5s
    max-worker-execute-time: 60s
    prefer-native-transport: true
    caching: false

  thread-pool:
    core-threads: 40                 # Match consumer concurrency (24) + headroom for other ops
    max-threads: 80                  # 2x core for burst traffic and latency spikes
    queue-size: 5000                 # Larger buffer to absorb TPS bursts without rejection
    growth-resistance: 0
    shutdown-timeout: 30s

  console:
    color: true
  log:
    level: INFO
    console:
      format: "[%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ}] %-5p [%c][%t][%X{traceId}][%X{userName}][%X{sessionId}] %s%e%n"
      async:
        queue-length: 10000
        overflow: discard # Fixed the "discard file not create" typo

    # File Configuration
    file:
      path: /var/log/dte/accounting-service-${HOSTNAME:unknown}.log
      format: "[%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ}] %-5p [%c][%t][%X{traceId}][%X{userName}][%X{sessionId}] %s%e%n"
      async:
        enable: true
        queue-length: 20000
        overflow: discard
      rotation:
        max-file-size: 100M
        max-backup-index: 15
        rotate-on-boot: true
      enabled: true

    category:
      "org.apache.kafka":
        level: INFO
      "io.smallrye.reactive.messaging.kafka":
        level: INFO
      "io.quarkus.config":
        level: INFO
      "io.smallrye.config":
        level: INFO
      "com.csg.airtel.aaa4j":
        level: INFO

  otel:
    traces:
      exporter: none

  # Micrometer Metrics Configuration
  micrometer:
    enabled: true
    binder:
      jvm: true
      system: true
      http-server: true
    export:
      prometheus:
        enabled: true
        path: /q/metrics


#  redis:
#    hosts: redis://localhost:6379
#    max-pool-size: 150               # Right-sized: 300 TPS x ~5ms = ~1.5 concurrent + headroom
#    max-waiting-handlers: 3000
#    max-pool-waiting: 1000
#    pool-recycle-timeout: 240000
#    reconnect-attempts: 3
#    reconnect-interval: 500
#    pool-cleaner-interval: 20000
#    response-timeout: 3000
#    connection-timeout: 2000

  redis:
    client-type: sentinel
    hosts: redis://redis-cluster-headless.csg-aaa-query-cache.svc.cluster.local:26379
    master-name: mymaster
    role: master
    password: S3cure_Redis_Pass_w0rd!
    max-pool-size: 150
    max-waiting-handlers: 3000
    max-pool-waiting: 1000
    pool-recycle-timeout: 240000
    reconnect-attempts: 3
    reconnect-interval: 500
    pool-cleaner-interval: 20000
    timeout: 120000

  # CoA HTTP Client Configuration (Vert.x WebClient)
#coa:
#  nas:
#    host: localhost
#    port: 8088

coa:
  nas:
#    host: airtel-aaa-radius-server-service.airtel-aaa.svc.cluster.local
    host: localhost
    port: 8088

webclient:
  max-pool-size: 20                  # Right-sized for 300 TPS/pod HTTP/1.1
  connect-timeout: 5000
  idle-timeout: 60000
  keep-alive: true
  pipelining: true
  pipelining-limit: 10
  http2-max-pool-size: 20            # Right-sized for 300 TPS/pod HTTP/2
  http2-multiplexing-limit: 100
  http2-keep-alive-timeout: 60


# Pool Configuration - Optimized for 1500 TPS (5 pods x 1 core, 300 TPS/pod)
# DB: 32 cores, 12GB RAM → max ~160 total connections safe
# Per pod: 30 connections x 5 pods = 150 total DB connections
pool:
  max-size: 30                      # 5 pods x 30 = 150 total (safe for 32-core DB, supports concurrency=24)
  connection-timeout: 5000          # 5 seconds to acquire connection
  idle-timeout: 600000              # 10 minutes idle before closing
  max-lifetime: 1800000             # 30 minutes max connection lifetime
  acquire-increment: 4              # Connections to acquire at once
  prepared-statement-cache-max-size: 256  # Cache prepared statements
  pipelining-enabled: true          # Enable query pipelining
  pipelining-limit: 256             # Max pipelined queries
  event-loop-size: 4                # Match vertx event-loops-pool-size
  tcp-keep-alive: true              # Detect dead connections
  tcp-no-delay: true                # Reduce latency
  pool-cleaner-enabled: true        # Clean idle connections
  pool-cleaner-interval: 60000      # Clean every minute


idle-session:
  enabled: true
  timeout-minutes: 60       # 1 hour default - sessions older than this will be terminated
  scheduler-interval: 30m    # Run every 60 minutes
  batch-size: 1000

  # Process 1000 sessions per batch (increased for scale)
kafka:
  bootstrap:
    servers: kafka-headless.cluster-dc.svc.cluster.local:9092


# Kafka Producer/Consumer Configuration - Optimized for 1500 TPS (5 pods x 1 core, 8 partitions, concurrency=24/pod)
mp:
  messaging:
    outgoing:
      db-write-events:
        connector: smallrye-kafka
        topic: dctodr-dc
        value.serializer: io.quarkus.kafka.client.serialization.ObjectMapperSerializer
        key.serializer: org.apache.kafka.common.serialization.StringSerializer
        acks: all
        retries: 5
        retry.backoff.ms: 500
        max.in.flight.requests.per.connection: 5
        compression.type: lz4
        batch.size: 16384              # 16KB - right-sized for 300 TPS/pod
        linger.ms: 5
        buffer.memory: 33554432        # 32MB - reduced for 1-core pod memory
        metadata.max.age.ms: 30000
        request.timeout.ms: 30000
        delivery.timeout.ms: 120000
        reconnect.backoff.ms: 1000
        reconnect.backoff.max.ms: 5000

      #        retries: 3
#        max.in.flight.requests.per.connection: 5
#        compression.type: snappy
#        batch.size: 32768
#        linger.ms: 5
#        buffer.memory: 67108864

      accounting-resp-events:
        connector: smallrye-kafka
        topic: accounting-response-dc
        value.serializer: io.quarkus.kafka.client.serialization.ObjectMapperSerializer
        key.serializer: org.apache.kafka.common.serialization.StringSerializer
        acks: all
        retries: 3
        max.in.flight.requests.per.connection: 5
        compression.type: lz4
        batch.size: 16384              # 16KB - right-sized for 300 TPS/pod
        linger.ms: 5
        buffer.memory: 33554432        # 32MB - reduced for 1-core pod memory

      accounting-cdr-events:
        connector: smallrye-kafka
        topic: cdr-event-dc
        value.serializer: io.quarkus.kafka.client.serialization.ObjectMapperSerializer
        key.serializer: org.apache.kafka.common.serialization.StringSerializer
        acks: all
        retries: 3
        max.in.flight.requests.per.connection: 5
        compression.type: lz4
        batch.size: 16384              # 16KB - right-sized for 300 TPS/pod
        linger.ms: 5
        buffer.memory: 33554432        # 32MB - reduced for 1-core pod memory

      quota-notification-events:
        connector: smallrye-kafka
        topic: quota-notifications-dc
        value.serializer: io.quarkus.kafka.client.serialization.ObjectMapperSerializer
        key.serializer: org.apache.kafka.common.serialization.StringSerializer
        acks: all
        retries: 3
        max.in.flight.requests.per.connection: 5
        compression.type: lz4
        batch.size: 16384              # 16KB - right-sized for 300 TPS/pod
        linger.ms: 5
        buffer.memory: 33554432        # 32MB - reduced for 1-core pod memory

    incoming:
      accounting-events:
        connector: smallrye-kafka
        topic: accounting-dc-1
        group.id: accounting-consumer-group
        value.deserializer: io.quarkus.kafka.client.serialization.ObjectMapperDeserializer
        key.deserializer: org.apache.kafka.common.serialization.StringDeserializer
        auto.offset.reset: earliest
        enable.auto.commit: false
        max.poll.records: 500           # Larger batches for higher throughput during backlog catch-up
        max.poll.interval.ms: 300000
        session.timeout.ms: 30000
        heartbeat.interval.ms: 10000    # Prevent consumer rebalancing under sustained high load
        fetch.min.bytes: 1             # Reduce latency - don't wait for min bytes
        fetch.max.bytes: 52428800
        max.partition.fetch.bytes: 10485760  # 10MB per partition - support larger batch fetches
        receive.buffer.bytes: 262144    # 256KB socket buffer for faster data transfer
        connections.max.idle.ms: 540000 # 9 min - keep broker connections alive under load
        failure-strategy: ignore
        concurrency: 24                 # 3x partition count - allows 24 msgs processed concurrently per pod


