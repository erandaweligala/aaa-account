# Server Configuration
quarkus:
  http:
    root-path: /api/v1
    port: 9905
    io-threads: 8
    limits:
      max-body-size: 10M
      max-connections: 2000


  datasource:
    db-kind: oracle
    username: aaa
    password: Aaa!89nky78D
    reactive:
      url: oracle:thin:@aaa-dev.cn8eyeqma3tp.ap-southeast-2.rds.amazonaws.com:1521/ORCL
      max-size: 50
      idle-timeout: PT10M
      acquisition-timeout: PT5S

  # Vertx Configuration - HIGH TPS OPTIMIZED for 2500 TPS
  vertx:
    event-loops-pool-size: 32
    warning-exception-time: 10s
    max-event-loop-execute-time: 5s
    max-worker-execute-time: 60s
    prefer-native-transport: true
    caching: true


  thread-pool:
    core-threads: 100
    max-threads: 500
    queue-size: 5000
    growth-resistance: 0
    shutdown-timeout: 30s

  console:
    color: true
  log:
    level: INFO
    file:
      path: /var/dte/aaa-account.log
      format: "%d{yyyy-MM-dd HH:mm:ss,SSS} %-5p [%c{3.}] (%t) %s%e%n"
      rotation:
        max-file-size: 100M
        max-backup-index: 10
        rotate-on-boot: true
    console:
      format: "%d{yyyy-MM-dd HH:mm:ss,SSS} %-5p [%c{3.}] (%t) %s%e%n"
    category:
      "org.apache.kafka":
        level: INFO
      "io.smallrye.reactive.messaging.kafka":
        level: INFO
      "io.quarkus.config":
        level: INFO
      "io.smallrye.config":
        level: INFO
      "com.csg.airtel.aaa4j":
        level: INFO

  otel:
    traces:
      exporter: none

  # Redis Configuration - HIGH TPS OPTIMIZED for 2500 TPS with 5M records
  redis:
    client-type: sentinel
    hosts: redis://sentinel1:26379,redis://sentinel2:26379,redis://sentinel3:26379
    master-name: mymaster  # Your sentinel master name
    role: master
    # Connection pool configuration for high throughput
    max-pool-size: 200              # Increased from default 64 for high concurrency
    max-waiting-handlers: 2000      # Queue for waiting requests
    max-pool-waiting: 50            # Max time to wait for connection (ms)
    pool-recycle-timeout: 300000    # Recycle connections after 5 minutes
    reconnect-attempts: 3           # Retry connection on failure
    reconnect-interval: 1000        # Wait 1s between reconnect attempts
    tcp-keep-alive: true            # Detect dead connections
    tcp-no-delay: true              # Reduce network latency

# Redis Server Configuration (configure on Redis server side)
# MEMORY MANAGEMENT for 5M records:
# Estimated: 5M records × 1KB avg = 5GB + 30% overhead = 6.5GB
# Recommended Redis server config (redis.conf):
#   maxmemory 8gb                          # Set limit with buffer
#   maxmemory-policy allkeys-lru           # Evict least recently used keys
#   maxmemory-samples 5                    # LRU approximation quality
# Note: Application uses 4h TTL for auto-cleanup, but LRU provides additional safety

# Pool Configuration
pool:
  max-size: 50                      # Max connections
  connection-timeout: 5000          # 5 seconds to acquire connection
  idle-timeout: 600000              # 10 minutes idle before closing
  max-lifetime: 1800000             # 30 minutes max connection lifetime
  acquire-increment: 4              # Connections to acquire at once
  prepared-statement-cache-max-size: 256  # Cache prepared statements
  pipelining-enabled: true          # Enable query pipelining
  pipelining-limit: 256             # Max pipelined queries
  event-loop-size: 32               # Match vertx event-loops-pool-size
  tcp-keep-alive: true              # Detect dead connections
  tcp-no-delay: true                # Reduce latency
  pool-cleaner-enabled: true        # Clean idle connections
  pool-cleaner-interval: 60000      # Clean every minute

idle-session:
  enabled: true
  timeout-minutes: 60       # 1 hour default - sessions older than this will be terminated
  scheduler-interval: 60m    # Run every 60 minutes
  batch-size: 1000          # Process 1000 sessions per batch (increased for scale)

# Kafka Configuration
kafka:
  bootstrap:
   servers: confluent-ctrl-0-svc.rkafkacone.svc.cluster.local:9092

mp:
  messaging:
    outgoing:
      db-write-events:
        connector: smallrye-kafka
        topic: DC-DR
        value.serializer: io.quarkus.kafka.client.serialization.ObjectMapperSerializer
        key.serializer: org.apache.kafka.common.serialization.StringSerializer
        acks: all
        retries: 3
        max.in.flight.requests.per.connection: 5
        compression.type: snappy
        batch.size: 32768
        linger.ms: 5
        buffer.memory: 67108864

      accounting-resp-events:
        connector: smallrye-kafka
        topic: accounting-response
        value.serializer: io.quarkus.kafka.client.serialization.ObjectMapperSerializer
        key.serializer: org.apache.kafka.common.serialization.StringSerializer
        acks: all
        retries: 3
        max.in.flight.requests.per.connection: 5
        compression.type: snappy
        batch.size: 32768
        linger.ms: 5
        buffer.memory: 67108864

      accounting-cdr-events:
        connector: smallrye-kafka
        topic: cdr-event
        value.serializer: io.quarkus.kafka.client.serialization.ObjectMapperSerializer
        key.serializer: org.apache.kafka.common.serialization.StringSerializer
        acks: all
        retries: 3
        max.in.flight.requests.per.connection: 5
        compression.type: snappy
        batch.size: 32768
        linger.ms: 5
        buffer.memory: 67108864

      quota-notification-events:
        connector: smallrye-kafka
        topic: quota-notifications
        value.serializer: io.quarkus.kafka.client.serialization.ObjectMapperSerializer
        key.serializer: org.apache.kafka.common.serialization.StringSerializer
        acks: all
        retries: 3
        max.in.flight.requests.per.connection: 5
        compression.type: snappy
        batch.size: 32768
        linger.ms: 5
        buffer.memory: 67108864

    incoming:
      accounting-events:
        connector: smallrye-kafka
        topic: accounting
        group.id: accounting-consumer-group
        value.deserializer: io.quarkus.kafka.client.serialization.ObjectMapperDeserializer
        key.deserializer: org.apache.kafka.common.serialization.StringDeserializer
        auto.offset.reset: earliest
        enable.auto.commit: false
        # HIGH TPS OPTIMIZATION: Increased from 500 to 1000 for better throughput
        max.poll.records: 1000
        max.poll.interval.ms: 300000
        session.timeout.ms: 30000
        fetch.min.bytes: 128
        fetch.max.bytes: 52428800
        max.partition.fetch.bytes: 10485760
        # Increased receive buffer for higher throughput
        receive.buffer.bytes: 131072
        failure-strategy: ignore

